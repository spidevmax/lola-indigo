{
  "name": "lola-indigo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch server/db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "json-server": "^1.0.0-beta.3"
  }
}
